///xIsRadiusTouching(other)
/// Returns true if the distance between the current instance and the specified
/// instance is smaller then the sprite width.

return (point_distance(x, y, argument0.x, argument0.y) < (abs(sprite_width) / 2 + abs(argument0.sprite_width) / 2));

