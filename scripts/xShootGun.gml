///xShootGun(gun, direction, enemy, accuracy)
/// Shoots the gun with the current instance.

for(var i = 0; i < argument0.num_bullets; i++) {
    xShootBullet(argument0, argument1, argument2, argument3);
}

